/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode : false,
    env: {
        customKey: 'https://i11b302.p.ssafy.io'
        // customKey: 'http://localhost:8080'
    }
};

export default nextConfig;
